# ChartPilot 需求规格说明

## 1. 文档目的

本文档记录 ChartPilot 当前已经明确的产品目标、使用场景、功能范围、运行约束和验收标准，作为后续技术选型、Skill 开发、离线打包和测试的共同依据。

## 2. 产品定位

ChartPilot 是一个运行在 Windows 本地环境中的数据分析 Agent。

用户通过自然语言提出分析问题，ChartPilot 读取本地 CSV 文件，由 Agent 基于 inspect、analysis、render 三份轻量模板生成任务特定 Python，使用随包 WinPython 完成数据检查、清洗、分析和可视化。最终交付图表图片、分析结论、完整执行代码及可追溯的中间结果。

产品主要面向不熟悉 SQL 或 Python，但需要快速从业务数据中获得指标、趋势、对比和原因线索的用户。

## 3. 已明确的关键约束

- 产品名称：ChartPilot
- 目标平台：Windows 10 / Windows 11
- 运行方式：Windows 原生运行
- 不依赖 WSL、Docker 或 Linux 子系统
- 用户环境可能无法访问互联网
- 运行时仅要求能够连接指定的 LLM API
- Agent、Python 运行时、依赖包和 Skill 必须支持离线拷贝与安装
- 第一阶段以本地 CSV 文件为主要数据源
- 数据处理和图表生成必须在用户本地完成
- 发送给 LLM 的数据范围必须可配置，具体允许范围仍需确认

## 4. 目标业务场景

### 4.1 指标查询

用户用自然语言查询汇总指标，例如：

> 帮我统计 2026 年 6 月华东地区的销售总额，并与上月对比。

系统应识别时间、区域、指标和比较口径，执行筛选和聚合，并返回结果及计算口径。

### 4.2 趋势与对比分析

用户查询指标变化趋势、分组差异或排名，例如：

> 展示最近 12 个月各区域销售额趋势，并找出增长最快的区域。

系统应完成时间字段处理、分组聚合、增长率计算，并选择合适的趋势图或排名图。

### 4.3 数据质量与基础清洗

系统应识别缺失值、重复记录、字段类型错误和明显异常值，并说明发现的问题及采用的处理方式。

系统不得覆盖原始 CSV。所有清洗动作必须可追溯；存在业务歧义时，优先提示用户确认，不得静默删除或修改数据。

### 4.4 原因线索分析

用户询问某项指标为何变化时，系统应从现有字段中按时间、地区、渠道、产品等维度进行拆解，找出贡献变化最大的分组并给出证据。

系统输出的是基于当前数据的贡献分析或相关性线索，不应把相关性描述为因果结论。

## 5. MVP 功能范围

### FR-01 自然语言任务输入

- 接收中文自然语言问题
- 接收一个本地 CSV 文件路径和一个输出目录
- 从问题中识别指标、维度、筛选条件、时间范围、排序方式和期望输出
- 当字段映射或业务口径存在关键歧义时，向用户发起澄清

### FR-02 CSV 读取与数据剖析

- 支持常见 CSV 分隔符和 UTF-8、UTF-8 BOM、GBK 等常见中文编码
- 读取表头、行列数、字段类型、缺失率、唯一值数量和少量样例
- 识别候选时间字段、数值字段、分类字段和标识字段
- 检测重复行、疑似主键重复、空列及常量列
- 大文件场景下先采样剖析，避免一次性把全部数据送入模型上下文
- 生成结构化的数据概况文件

### FR-03 分析计划生成

- 根据用户问题和数据概况生成可执行的分析计划
- 明确使用的字段、筛选条件、聚合方法、计算公式和预期图表
- 在执行前检查用户问题所需字段是否存在
- 无法完成时明确说明缺少的字段或口径，不得编造结果

### FR-04 Python 代码生成与本地执行

- 使用 Python 和 pandas 完成数据读取、清洗、筛选、聚合和统计计算
- 所有 CSV 任务都通过 Agent 修改三份轻量模板并执行任务目录内的完整 Python 源码
- 不保留白名单声明式计划、固定 runner 或固定图表选择器作为备用路由
- 所有 Python 必须通过 ChartPilot 随包携带的固定解释器运行，不依赖系统 Python
- 捕获标准输出、错误信息、退出码和执行耗时
- 失败后允许根据错误信息进行有限次数的修正与重试
- 本阶段不使用 AST/import 白名单或 Python 沙箱；生成代码拥有当前 Windows 用户权限
- 保存最终执行代码，保证分析过程可审计、可复现

### FR-05 数据清洗

- 识别并处理字段类型、缺失值、重复值和明显异常值问题
- 清洗策略应根据分析目标选择，不能统一采用删除或填充
- 原始文件只读，清洗结果写入新的中间文件
- 输出清洗前后行数变化、处理规则和受影响记录数量

### FR-06 数据分析

- 支持求和、均值、计数、去重计数、最大值、最小值和占比等常用指标
- 支持筛选、分组、排序、Top N、同比、环比和时间趋势
- 支持不同地区、产品、渠道或其他分类维度的对比
- 支持对指标变化进行维度拆解和贡献度分析
- 输出结构化结果表和自然语言结论

### FR-07 智能图表选择与绘制

- 图表选择优先匹配分析意图和数据角色，而不是字段名或单个关键词
- 同一类“分组同群基准 + 阈值风险分级 + 气泡规模”请求应复用一致的图表原型：数量构成
  与聚合指标/基准阈值使用协同面板表达
- 生成 render 源码必须与 analysis 阶段记录的原型、面板粒度和编码计划一致；不匹配的请求
  可采用自由方案，但需说明不适用原因
- 趋势数据优先使用折线图
- 分类对比和排名优先使用条形图
- 构成占比可使用饼图或环形图；分类过多时改用条形图
- 数值分布优先使用直方图、箱线图或散点图
- 使用 matplotlib 或 seaborn 在本地生成图表
- 支持中文标题、坐标轴和图例正常显示
- 对标签过多、量纲差异和极端值等情况进行可读性处理
- 图表必须标明标题、指标单位、时间范围和必要的数据来源说明

### FR-08 结果交付

每次成功任务至少输出：

- 一张 PNG 图表图片
- 一份结构化分析结果表（CSV 或 JSON）
- 一段中文分析摘要
- 一份本次执行的 Python 代码
- 一份执行日志或任务元数据

可选支持返回图片 Base64，但本地图片文件是 MVP 的必选交付物。

### FR-09 结果校验

- 图表使用的数据必须来自已保存的分析结果
- 摘要中的关键数字应能在结果表中核对
- 执行结束后检查图片文件存在、尺寸有效且不是空白图
- 对除零、空结果、日期解析失败和数值转换失败等情况给出明确提示

## 6. 推荐执行流程

```text
用户问题 + CSV 路径
        |
        v
准备 request、task context 与三份模板
        |
        v
Agent 修改 inspect 模板并执行数据探查
        |
        v
Agent 修改 analysis 模板并执行任务特定计算
        |
        v
验证结果契约并按需修改代码重试
        |
        v
Agent 修改 render 模板并生成图表和摘要
        |
        v
保存代码、日志和任务元数据
```

## 7. Skill 边界

产品只提供 `chartpilot-run-python` 一个自适应 Skill，避免多个相似 Skill 导致错误路由。
Skill 管理 prompt+CSV 准备、三份可编辑模板、阶段顺序、产物契约和视觉迭代规则；明细输出
粒度与图表粒度分别决策。Agent 先核对生成 Python 是否实现 analysis 阶段选择的图表原型，
再实际读取最终 PNG；源码结构和图片可读性均通过后才能完成任务。

Goose 底座负责对话和模型 Provider；ChartPilot MCP 只暴露任务准备与任务 Python 执行两个
工具，负责运行时定位、环境清理、staging、超时、执行记录和产物验证。字段语义、业务计算、
阈值和图表布局由 Agent 编写的阶段代码负责。

## 8. 非功能要求

### 8.1 离线与可部署性

- 安装包应包含固定 Goose Desktop 1.43.0 无 CUDA Agent、固定 WinPython 运行时、哈希锁定依赖、Skill、MCP 和配置模板
- 在无外网环境中完成安装、启动和数据分析
- 所有运行依赖必须提前锁定版本并进行离线兼容测试
- LLM API 地址、密钥、模型名、超时和重试次数可配置

### 8.2 安全与隐私

- 默认只允许读取用户指定文件及工作目录
- 默认只允许写入指定输出目录
- 本阶段不实现生成 Python 的网络隔离；需要限制时由部署环境的网络策略承担
- API Key 不写入日志、生成代码或分析结果
- 发送给 LLM 的上下文应遵循最小化原则；允许范围由部署配置决定
- 所有生成代码和执行记录均保留审计信息
- 默认 Goose 配置不得启用通用 Developer 扩展；标准 CSV 流程只使用两个自适应 MCP 工具
- Goose 与 WinPython 均不得被描述为 Windows 操作系统沙箱

### 8.3 稳定性

- 单次代码执行应具备超时限制
- 重试次数应可配置并有上限
- 任务失败不得破坏原始文件和已完成的结果
- 文件编码、中文路径、空格路径和常见 Windows 权限错误应有明确处理

### 8.4 可维护性

- Agent 底座、业务 Skill、Python 运行时和模型配置相互解耦
- Skill 应可独立测试和版本管理
- 生成物目录结构固定，便于问题定位和结果归档
- 关键输入输出使用明确的数据结构和版本号

## 9. 建议的单次任务输出目录

```text
workspace/
  tasks/
    <task_id>/
      request.md
      task_context.json
      generated_inspect.py
      inspection.json
      generated_analysis.py
      result.csv
      <optional-declared-analysis-artifacts>
      analysis_result.json
      generated_chart.py
      chart.png
      chart_result.json
      summary.md
      executions/
```

## 10. MVP 范围外事项

以下能力不作为第一版必须交付项：

- 直接连接业务数据库并执行 SQL
- Excel、Parquet 等多格式数据源
- 同时关联多个 CSV 文件
- 交互式图表和仪表盘
- 自动生成完整分析报告或演示文稿
- 定时任务和批量任务调度
- 基于领域知识作严格因果推断
- 多用户、权限中心和服务端部署

这些能力应在 CSV 单文件分析链路稳定后逐步扩展。

## 11. MVP 验收标准

在一台无 WSL、无 Docker、除 LLM API 外无互联网访问的 Windows 测试机上，完成以下验收：

1. 解压或离线安装后能够启动 ChartPilot。
2. 配置 LLM API 后能够进行中文自然语言交互。
3. 能读取包含中文路径、中文表头和中文内容的本地 CSV。
4. 能正确输出字段概况、缺失情况和数据样例。
5. 对给定指标查询，结果与人工使用 pandas 计算的结果一致。
6. 能执行至少一种趋势分析、一种分类对比和一种 Top N 分析。
7. 能生成清晰、中文不乱码且内容非空的 PNG 图表。
8. 能输出包含关键数字、口径和主要发现的中文摘要。
9. 能保存结果表、生成代码和执行日志，并可根据这些文件复现计算。
10. 遇到字段缺失、空数据或代码执行失败时，能够给出可理解的错误信息，且不修改原始 CSV。
11. 默认模板不主动访问网络；本阶段不把网络隔离作为生成代码验收项。
12. API Key 和原始数据内容不出现在日志中。

## 12. 当前待确认的产品决策

以下内容尚不影响 MVP 需求定义，但需要在实现前或技术验证阶段确定：

- LLM API 采用哪一种协议及模型，上下文长度和工具调用能力要求
- 允许发送给 LLM 的数据范围：仅字段元数据、脱敏样例，还是受控的小规模原始数据
- MVP 支持的最大 CSV 文件大小和最长任务执行时间
- 后续是否需要在 MCP 结果中直接返回图片内容，而不是当前 PNG 路径与哈希

已确定：Agent 底座采用 Goose Desktop `v1.43.0` Windows x64 无 CUDA ZIP；第一版提供
图形界面；标准 CSV 流程通过两个 MCP 工具执行 Agent 编写的任务 Python，不提供旧的确定性
工具或通用 shell。该执行路径不是操作系统沙箱。

这些决策确定后，应补充到本文档并转化为可测试的配置项或验收指标。
