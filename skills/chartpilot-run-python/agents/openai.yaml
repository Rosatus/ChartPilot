interface:
  display_name: "ChartPilot Adaptive CSV"
  short_description: "Role-matched CSV reports with adaptable Python"
  default_prompt: "Use $chartpilot-run-python to inspect this CSV, select a role-matched visual archetype, write task-specific analysis and render Python, run it with bundled WinPython, review the generated source and chart, and return committed artifacts."
