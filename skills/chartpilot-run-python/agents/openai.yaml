interface:
  display_name: "ChartPilot Python Runtime"
  short_description: "Author and run audited Python with the bundled runtime"
  default_prompt: "Use $chartpilot-run-python to author and execute Python with ChartPilot's bundled runtime."
