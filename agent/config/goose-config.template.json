{
  "GOOSE_MODE": "smart_approve",
  "GOOSE_TELEMETRY_ENABLED": false,
  "GOOSE_DISABLE_SESSION_NAMING": true,
  "extensions": {
    "summon": {
      "enabled": true,
      "type": "builtin",
      "name": "summon",
      "display_name": "Summon",
      "description": "Load the bundled ChartPilot Agent Skills.",
      "timeout": 300,
      "bundled": true,
      "available_tools": []
    },
    "chartpilot": {
      "enabled": true,
      "type": "stdio",
      "name": "chartpilot",
      "display_name": "ChartPilot",
      "description": "Adaptive prompt-plus-CSV analysis with task-specific Python and charts.",
      "cmd": "${CHARTPILOT_PYTHON}",
      "args": [
        "-I",
        "${CHARTPILOT_MCP_SERVER}"
      ],
      "cwd": "${CHARTPILOT_ROOT}",
      "env_keys": [],
      "envs": {
        "CHARTPILOT_ROOT": "${CHARTPILOT_ROOT}",
        "CHARTPILOT_WORKSPACE_ROOT": "${CHARTPILOT_WORKSPACE_ROOT}",
        "CHARTPILOT_ALLOWED_READ_ROOTS": "${CHARTPILOT_ALLOWED_READ_ROOTS}"
      },
      "timeout": 300,
      "bundled": true,
      "available_tools": [
        "chartpilot_prepare_adaptive_task",
        "chartpilot_run_task_python"
      ]
    }
  }
}
